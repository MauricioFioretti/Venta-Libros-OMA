const Carrito = () => {
    return (
        <div>
            <img src={`${process.env.PUBLIC_URL}/img/carrito.svg`} alt="carrito-compras"></img>
            0
        </div>
    )
}

export default Carrito